# __PROJECT_NAME__

Production-oriented SaaS starter generated by `create-saas-stack`.

## Quick start

1. Copy `.env.example` to `.env` at the repository root and fill values.
2. `pnpm install`
3. `pnpm db:migrate`
4. `pnpm dev`
5. In another terminal, `pnpm worker`

> Important: Clerk requires `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` for the browser and `CLERK_SECRET_KEY` for server routes. Keep both in the root `.env`.

## Billing setup (Stripe + Supabase Postgres)

Billing is disabled unless `STRIPE_BILLING_ENABLED=true`.

For subscriptions and product checkout:
- Configure `STRIPE_PRICE_PRO` and `STRIPE_PRICE_ULTRA`.
- Configure optional one-time price such as `STRIPE_PRICE_BOOST_PACK`.
- Run migrations before accepting live checkout requests.

The billing page includes starter plan/product cards with placeholder feature lists (`FREE`, `PRO`, `ULTRA`, and a one-time purchase example).

## Webhooks

- Stripe webhook: `/api/stripe/webhook`
- Clerk webhook: `/api/clerk/webhook`

Use Stripe CLI for local forwarding:

```bash
stripe listen --forward-to localhost:3000/api/stripe/webhook
```

## Providers

- Clerk: email-first auth, orgs and memberships
- Stripe: hosted checkout + webhooks as source of truth
- Loops: event delivery through worker queue
- Supabase: Postgres + Storage signed uploads
